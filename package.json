{
  "name": "meditrack-pro",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm --workspace @meditrack/api run dev\" \"npm --workspace @meditrack/web run dev\"",
    "build": "npm run typecheck && npm run --workspaces --if-present build",
    "typecheck:packages": "tsc --build",
    "typecheck": "npm run typecheck:packages && npm run --workspaces --if-present typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  },
  "overrides": {
    "@esbuild-kit/esm-loader": "npm:tsx@^4.21.0",
    "esbuild": "0.27.3"
  }
}
